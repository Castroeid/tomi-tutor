/**
 * Placeholder for future AI integration.
 *
 * In the future, connect this frontend to a backend proxy endpoint
 * that talks to OpenAI securely.
 */
export async function requestFutureAiConversation(payload) {
  console.info("Future AI payload:", payload);
  return {
    ok: false,
    message: "Future integration placeholder."
  };
}
